import 'package:flutter/material.dart';

//Títulos
const erroCriacao = Text("Erro ao criar arquivo!");

//Caixa de Diálogo
final dlgSuccess = Text("Sucesso!!",
    style: TextStyle(
      color: Colors.green[700],
      fontWeight: FontWeight.bold,
    ));

final dlgError = Text("Erro!!",
    style: TextStyle(
      color: Colors.red[700],
      fontWeight: FontWeight.bold,
    ));

final dlgWarning = Text("Aviso!!",
    style: TextStyle(
      color: Colors.yellow[700],
      fontWeight: FontWeight.bold,
    ));
